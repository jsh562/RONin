# Testing Requirements Checklist: CI Quality Gates
**Created**: 2026-06-11 | **Feature**: [spec.md](../spec.md)

## Gate Coverage Completeness (every §V invariant gated)

- [X] CHK001 Does a requirement gate every project-instructions §V mandated check (rustfmt, clippy -D warnings, the test suite, the wasm32 ron-core build, AND supply-chain scanning), with none left ungated? [Completeness, Spec §OR-001..005] <!-- Evaluator: Covered by spec.md §OR-001 (fmt), OR-002 (clippy), OR-003 (tests), OR-004 (wasm32), OR-005 (supply-chain) — one MUST-fail gate per §V check -->
- [X] CHK002 Is rustfmt verification specified as `--check` (fail on drift) rather than an auto-formatting step that could silently rewrite and pass? [Clarity, Spec §OR-001] <!-- Evaluator: Covered by spec.md §OR-001 "cargo fmt --check ... a formatting violation MUST fail the run" + plan Coverage Map -->
- [X] CHK003 Is the clippy gate scoped to `--workspace --all-targets -- -D warnings`, so lints in tests/benches/examples cannot escape the gate? [Completeness, Spec §OR-002] <!-- Evaluator: Covered by spec.md §OR-002 "cargo clippy --workspace --all-targets -- -D warnings" -->
- [X] CHK004 Is the failure semantics of each gate stated as an absolute (a violation MUST fail the run), leaving no warn-only or advisory path for a mandated check? [Clarity, Spec §OR-001/OR-002/OR-003/OR-004] <!-- Evaluator: Covered by spec.md §OR-001/002/003/004 — each uses "MUST fail the run"; OR-005 "MUST hard-fail" -->
- [X] CHK005 Is coverage explicitly designated advisory (no threshold, not a gate), so its absence as a gate is a deliberate, traceable decision rather than an omission? [Completeness, Spec §Scope-Excluded, PI §Testing & Quality Policy] <!-- Evaluator: Covered by spec.md §Excluded "coverage is advisory per project policy (no threshold); a coverage job is optional, not a gate"; PI §Testing & Quality Policy "Coverage Target: none"; plan Testing Strategy "Coverage | N/A | advisory only — not gated" -->
- [X] CHK006 Is the test-suite gate scoped to the whole workspace (`--workspace`), so no crate's tests are silently skipped from the gate? [Completeness, Spec §OR-003/Plan §Coverage-Map] <!-- Evaluator: Covered by spec.md §OR-003 + plan Coverage Map "cargo test --workspace --locked" -->

## Test-Matrix Breadth Specification

- [X] CHK007 Is the OS breadth for the test job fully enumerated (Windows, macOS, AND Linux) with any-OS failure failing the run? [Completeness, Spec §OR-003/SC-002] <!-- Evaluator: Covered by spec.md §OR-003 "on Windows, macOS, and Linux; any test failure on any OS MUST fail the run" + SC-002 -->
- [X] CHK008 Is the boundary between per-OS jobs (test) and Linux-only jobs (fmt, clippy, wasm32, supply-chain) stated unambiguously, so matrix breadth is not over- or under-claimed? [Clarity, Spec §OR-001/OR-002/AD-006] <!-- Evaluator: Covered by spec.md §OR-001/OR-002 "(once, on Linux — OS-independent)" + Clarifications Q4 + plan AD-006/HINT-004 -->
- [X] CHK009 Was the prior inconsistency (SC-002 / OBJ1 VC2 claiming fmt/clippy on all three OSes) resolved so the matrix-breadth claim is internally consistent? [Consistency, Spec §STF-001/SC-002] <!-- Evaluator: Covered by spec.md §STF-001 (Resolution applied inline) + reworded SC-002/OBJ1 VC2 scoping per-OS to the test job -->
- [X] CHK010 Is the free-tier-minutes rationale for running OS-independent jobs once on Linux traceable to a decision, rather than the narrowing appearing arbitrary? [Traceability, Plan §AD-006/Spec §OBJ1] <!-- Evaluator: Covered by plan AD-006 "conserve free-tier minutes (spec Q4)" + spec OBJ1 deliverable + Clarifications Q4 -->
- [X] CHK011 Is cross-OS test determinism addressed as a requirement (path separators, CRLF vs LF must not cause spurious failures), so matrix breadth does not introduce flake that masks real gating? [Completeness, Spec §Edge Cases/Plan §Risk] <!-- Evaluator: Covered by spec.md §Edge Cases "Cross-OS differences ... must not cause spurious test failures" + plan Risk Mitigation row (deterministic tests + CRLF/BOM handling) -->

## Fuzz-Crate Exclusion vs Proptest Fallback

- [X] CHK012 Is the exclusion of the workspace-excluded `fuzz` crate from the stable build/test matrix stated as a requirement, and is the exclusion mechanism (`--workspace` + root `[workspace].exclude`) identified? [Completeness, Spec §OR-009/Plan §HINT-002] <!-- Evaluator: Covered by spec.md §OR-009 + plan Coverage Map OR-009 / HINT-002 "--workspace + root [workspace].exclude keeps fuzz out" -->
- [X] CHK013 Is it specified that excluding the fuzz crate must not break the run (it needs nightly/cargo-fuzz), so the exclusion is a clean boundary rather than a silent failure? [Clarity, Spec §Edge Cases/OR-009] <!-- Evaluator: Covered by spec.md §Edge Cases "must not be built on the stable matrix (it needs nightly/cargo-fuzz) and must not break the run" + Operational Constraints -->
- [X] CHK014 Is the no-panic / round-trip intent of fuzzing stated as still covered on the stable matrix via the proptest fallback in the test suite, so deferring nightly cargo-fuzz leaves no coverage gap? [Completeness, Spec §Scope-Excluded, PI §Testing & Quality Policy] <!-- Evaluator: Covered by spec.md §Excluded "the stable proptest fallback already runs in the test suite"; PI §Testing & Quality Policy mandates proptest round-trip invariants in the gated suite -->
- [X] CHK015 Is the deferral of the nightly ≥1M-iteration `cargo fuzz` run to a separate optional workflow stated explicitly, so it is a recorded scope decision rather than an undocumented omission? [Clarity, Spec §Scope-Excluded] <!-- Evaluator: Covered by spec.md §Excluded "The nightly cargo fuzz ≥1M-iteration run — requires the nightly toolchain; deferred to an optional separate workflow" -->
- [X] CHK016 Is fuzz-crate exclusion made verifiable via a success criterion (the crate is never built/tested by the stable matrix, checked against command scope / workspace excludes)? [Testability, Spec §SC-007] <!-- Evaluator: Covered by spec.md §SC-007 "fuzz crate is never built or tested by the stable CI matrix (verified by the matrix command scope / workspace excludes)" -->

## Verifiability of Red-on-Failure / Green-on-Clean

- [X] CHK017 Is "red on failure" defined as an observable, per-job outcome (the specific gate job goes red) rather than a generic "CI fails"? [Testability, Spec §SC-001/OBJ1 VC1] <!-- Evaluator: Covered by spec.md §SC-001 "produces a red gate-job check" + OBJ1 VC1 "the corresponding gate job fails (red)" -->
- [X] CHK018 Does SC-001 enumerate each failure trigger (formatting, a clippy lint, OR any OS's tests) so the red-on-failure criterion is testable per gate? [Completeness, Spec §SC-001] <!-- Evaluator: Covered by spec.md §SC-001 "fails formatting, a clippy lint, or any OS's tests" -->
- [X] CHK019 Is "green on clean" defined with the exact expected passing set (test job green on all three OSes; fmt/clippy/wasm32 green on Linux), so a partial pass cannot be mistaken for full green? [Clarity, Spec §SC-002] <!-- Evaluator: Covered by spec.md §SC-002 "the test job on Windows, macOS, and Linux, and the fmt/clippy/wasm32 jobs on Linux" -->
- [X] CHK020 Is a failure required to be actionable/observable (the offending advisory, license, or failing job surfaced), so red is diagnosable and not opaque? [Testability, Spec §SC-004/OBJ3 VC1] <!-- Evaluator: Covered by spec.md §SC-004 + OBJ3 VC1 "fails with the offending advisory/license reported" -->

## Pipeline Self-Validation

- [X] CHK021 Is workflow-YAML linting (actionlint) specified as part of the pipeline's own verification, so the gate definitions are themselves validated for syntax/expressions/job graph? [Completeness, Plan §Testing Strategy] <!-- Evaluator: Covered by plan Testing Strategy "Workflow lint | actionlint | Validate ci.yml syntax/expressions/job graph" — a named Implement/QC verification obligation -->
- [X] CHK022 Is a real end-to-end pipeline run specified as the integration test (green on a clean commit; red when fmt/clippy/test/wasm/advisory failures are injected), so the gates are proven, not merely declared? [Testability, Plan §Testing Strategy] <!-- Evaluator: Covered by plan Testing Strategy "Integration | GitHub Actions run | Whole pipeline green on a clean commit; red when fmt/clippy/test/wasm/advisory failures are injected" — a named Implement/QC obligation -->
- [X] CHK023 Is the install/pinning of the actionlint tool and other CI tooling specified consistently with the SHA-pin policy, so the self-validation step is itself reproducible? [Consistency, Plan §Testing Strategy/AD-003/AD-005] <!-- Evaluator: Covered by plan Testing Strategy "taiki-e/install-action actionlint" + AD-003 (pinned tool install) + AD-005 ("Pin every third-party action by full commit SHA") + HINT-001 -->
- [X] CHK024 Is local reproduction of the pipeline addressed (raw fmt/clippy/test/wasm32 commands via RR-003, optional `act`), so a CI failure can be reproduced and the verification design re-checked off-CI? [Completeness, Spec §RR-003/Plan §Testing Strategy] <!-- Evaluator: Covered by spec.md §RR-003 (runbook for local repro of exact fmt/clippy/test/wasm32 commands) + plan Testing Strategy "nektos/act optional ... runbook (RR-003) documents the raw cargo commands" -->

## Reproducibility of Test Runs

- [X] CHK025 Is the committed-`Cargo.lock` requirement paired with a mandated `--locked` on build/test, so test runs resolve a fixed dependency set rather than drifting? [Completeness, Spec §OR-007/SC-005] <!-- Evaluator: Covered by spec.md §OR-007 "Cargo.lock MUST be committed and CI MUST build/test with --locked" + SC-005 -->
- [X] CHK026 Is "no silent dependency drift" stated as a verifiable outcome (re-running CI on the same commit resolves an identical dependency set)? [Testability, Spec §SC-006] <!-- Evaluator: Covered by spec.md §SC-006 "Re-running CI on the same commit resolves an identical dependency set (no silent dependency drift)" -->
- [X] CHK027 Is the consistent stable-toolchain pin (`rust-toolchain.toml` across all jobs) required, so the test matrix runs on one defined toolchain and results are comparable across OSes? [Consistency, Spec §OR-008/Plan §AD-001] <!-- Evaluator: Covered by spec.md §OR-008 "use the pinned stable toolchain (rust-toolchain.toml) consistently across all jobs" + plan AD-001 -->
- [X] CHK028 Is the ordering dependency stated (Cargo.lock committed before CI uses `--locked`), so a missing lockfile is a defined failure rather than a surprise red? [Clarity, Spec §OR-007/Plan §HINT-003] <!-- Evaluator: Covered by plan HINT-003 "Ensure Cargo.lock is committed before CI uses --locked (OR-007), else CI fails on a missing lockfile" -->

## Cache Correctness (must not mask failures)

- [X] CHK029 Is the cache-key derivation defined (toolchain + lockfile + OS/target), so a stale cache cannot mask a dependency or toolchain change relevant to a gate result? [Completeness, Spec §OR-006/Plan §AD-002] <!-- Evaluator: Covered by spec.md §OR-006 "keyed on toolchain, lockfile, and OS/target" + plan AD-002 "per-OS/target keys" -->
- [X] CHK030 Is the warm-vs-cold speedup stated as a measurable, falsifiable outcome rather than a vague "faster", so cache value is verifiable without hiding a regression? [Testability, Spec §SC-005/OBJ4 VC1] <!-- Evaluator: Covered by spec.md §SC-005 "A warm-cache CI run is measurably faster than a cold run" + OBJ4 VC1 -->
- [X] CHK031 Is per-OS/per-target cache separation required, so one OS's cache cannot leak into another and produce a false pass? [Consistency, Spec §OR-006/Plan §AD-002] <!-- Evaluator: Covered by spec.md §OR-006 (keyed on OS/target) + plan AD-002 "Swatinem/rust-cache, per-OS/target keys" -->
- [X] CHK032 Is caching scoped as a P2 optimization that the OBJ1–OBJ3 gates function correctly without, so a cache fault cannot be a load-bearing part of a gate's verdict? [Clarity, Spec §OBJ4 "Why this priority"] <!-- Evaluator: Covered by spec.md §OBJ4 (Priority: P2) "Why this priority ... the gates in OBJ1–OBJ3 function correctly without it" -->

## OR-to-SC Traceability and Verifiability

- [X] CHK033 Does each operational requirement (OR-001..OR-012) map to at least one verifiable success criterion or verification criterion, with no OR left unverifiable? [Traceability, Spec §OR-001..012/SC-001..010] <!-- Evaluator: Covered — OR-001/002/003 -> SC-001/002 + OBJ1 VC; OR-004 -> SC-003; OR-005 -> SC-004; OR-006 -> SC-005; OR-007 -> SC-005/006; OR-008 -> SC-002/006 (toolchain comparability); OR-009 -> SC-007; OR-010 -> SC-008; OR-011 -> SC-010; OR-012 -> SC-009 -->
- [X] CHK034 Are OR-009 (fuzz exclusion) and OR-010 (named selectable jobs) now backed by dedicated success criteria (SC-007, SC-008) rather than relying on incidental coverage? [Traceability, Spec §OR-009/OR-010/SC-007/SC-008/Clarifications] <!-- Evaluator: Covered by spec.md §SC-007 (fuzz crate never built/tested) + SC-008 (named gate jobs individually selectable) + Clarifications Q5 "added SC-007 ... and SC-008" -->
- [X] CHK035 Is the wasm32 gate's regression-detection behavior verifiable both ways (a non-WASM-clean change fails; a WASM-clean change passes), so the gate is proven discriminating, not just present? [Testability, Spec §SC-003/OBJ2 VC1-2] <!-- Evaluator: Covered by spec.md §SC-003 "non-WASM-clean fails the wasm32 job; a WASM-clean change passes it" + OBJ2 VC1 (fail) / VC2 (pass) -->
- [X] CHK036 Are all gate jobs (fmt, clippy, per-OS test, wasm32, supply-chain) required to exist as discrete, individually selectable named checks, so each can be verified and configured as a required status check? [Completeness, Spec §OR-010/SC-008] <!-- Evaluator: Covered by spec.md §OR-010 "expose discrete, named jobs (fmt, clippy, test-per-OS, wasm32, supply-chain)" + SC-008 "each individually selectable as a required status check" -->
- [X] CHK037 Is the boundary between CI verifying that gate jobs exist and go red, versus enforcing them as merge blocks (the RR-001 admin step), drawn consistently so no SC claims CI owns branch protection? [Consistency, Spec §Clarifications/SC-001/RR-001] <!-- Evaluator: Covered by spec.md §Clarifications Q2 + SC-001/SC-002/OBJ1 VC1 parentheticals ("Enforcing red checks as merge blocks is the RR-001 branch-protection admin step") + RR-001 + STF-001 closing note -->

## Commit-Independent Detection Testability

- [X] CHK038 Is the scheduled scan's commit-independent purpose stated (detect newly published advisories with no new commit), distinct from the per-change scan? [Clarity, Spec §OR-005/OBJ3/Edge Cases] <!-- Evaluator: Covered by spec.md §OBJ3 deliverable "scheduled (cron) run ... so newly published advisories are detected without a new commit" + Edge Cases "A new security advisory published between merges — caught by the scheduled scan even with no new commits" -->
- [X] CHK039 Is a measurable outcome defined for the scheduled scan surfacing a new advisory on its next run, making commit-independent detection latency verifiable? [Testability, Spec §SC-004/Plan §AD-004] <!-- Evaluator: Covered by spec.md §SC-004 + OBJ3 VC2 "surfaces any newly published advisory on the next scheduled run" + plan AD-004 "Daily cron (00:00 UTC) — low advisory-detection latency" -->
- [X] CHK040 Is the scheduled scan required to run the same checks as the per-change scan, so schedule and per-PR verification coverage cannot diverge? [Consistency, Spec §OR-005/OBJ3] <!-- Evaluator: Covered by spec.md §OR-005 "AND on a recurring schedule" (same single supply-chain job) + OBJ3 deliverable "A scheduled (cron) run of the same job" + plan Coverage Map OR-005 "one job ... on PR/push + daily cron" -->
